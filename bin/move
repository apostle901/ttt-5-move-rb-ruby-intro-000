#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = Array.new(9, " ")
puts "Welcome to Tic Tac Toe!"
display_board(board)
puts "What move would you like to make 1-9?"
move = gets.chomp
board = move(board, input_to_index(move), 'X')
display_board(board)
